@extends('layouts.admin')
@section('title','Gerakan') @section('page-title','Manajemen Gerakan')
@section('content')
<div class="admin-toolbar"><form method="GET" class="admin-search"><i data-lucide="search" class="h-4 w-4"></i><input type="search" name="q" value="{{ request('q') }}" placeholder="Cari gerakan..."><button class="btn-table" type="submit">Cari</button></form><a href="{{ route('admin.gerakan.create') }}" class="btn-primary"><i data-lucide="plus" class="h-4 w-4"></i> Tambah Gerakan</a></div>
<div class="admin-table-wrap mt-5"><table class="admin-table"><thead><tr><th>Gerakan</th><th>Kategori</th><th>Urutan</th><th>Bacaan</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($items as $item)<tr><td><div class="flex min-w-64 items-center gap-3"><img src="{{ $item->gambar_src }}" alt="" class="h-14 w-16 rounded-xl bg-slate-50 object-cover"><div><strong>{{ $item->nama }}</strong><small class="block text-slate-500">{{ $item->slug }}</small></div></div></td><td>{{ $item->kategori?->nama }}</td><td>{{ $item->urutan }}</td><td>{{ $item->bacaan_count }}</td><td><span class="status-badge {{ $item->status_aktif ? 'active' : '' }}">{{ $item->status_aktif ? 'Aktif' : 'Nonaktif' }}</span></td><td><div class="table-actions"><a href="{{ route('admin.gerakan.edit',$item) }}" class="btn-table">Edit</a><form method="POST" action="{{ route('admin.gerakan.destroy',$item) }}" onsubmit="return confirm('Hapus gerakan, bacaan, dan medianya?')">@csrf @method('DELETE')<button class="btn-table danger">Hapus</button></form></div></td></tr>@empty<tr><td colspan="6" class="text-center text-slate-500">Belum ada gerakan.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $items->links() }}</div>
@endsection
