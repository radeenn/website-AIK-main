@extends('layouts.admin')
@section('title','Kategori') @section('page-title','Kategori Pengguna')
@section('content')
<div class="admin-toolbar"><p>Kelola mode Dewasa dan Anak-anak.</p><a href="{{ route('admin.kategori.create') }}" class="btn-primary"><i data-lucide="plus" class="h-4 w-4"></i> Tambah Kategori</a></div>
<div class="admin-table-wrap mt-5"><table class="admin-table"><thead><tr><th>Kategori</th><th>Slug</th><th>Warna</th><th>Gerakan</th><th>Aksi</th></tr></thead><tbody>@forelse($items as $item)<tr><td><strong>{{ $item->nama }}</strong><small class="block max-w-md text-slate-500">{{ $item->deskripsi }}</small></td><td><code>{{ $item->slug }}</code></td><td><span class="inline-flex items-center gap-2"><i class="h-5 w-5 rounded-full border" style="background:{{ $item->warna }}"></i>{{ $item->warna }}</span></td><td>{{ $item->gerakan_count }}</td><td><div class="table-actions"><a href="{{ route('admin.kategori.edit',$item) }}" class="btn-table">Edit</a><form method="POST" action="{{ route('admin.kategori.destroy',$item) }}" onsubmit="return confirm('Kategori dan seluruh gerakan terkait akan dihapus. Lanjutkan?')">@csrf @method('DELETE')<button class="btn-table danger">Hapus</button></form></div></td></tr>@empty<tr><td colspan="5" class="text-center text-slate-500">Belum ada kategori.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $items->links() }}</div>
@endsection
