@extends('layouts.admin')
@section('title','Bacaan') @section('page-title','Manajemen Bacaan dan Audio')
@section('content')
<div class="admin-toolbar"><form method="GET" class="admin-search"><i data-lucide="search" class="h-4 w-4"></i><input type="search" name="q" value="{{ request('q') }}" placeholder="Cari judul bacaan..."><button class="btn-table">Cari</button></form><a href="{{ route('admin.bacaan.create') }}" class="btn-primary"><i data-lucide="plus" class="h-4 w-4"></i> Tambah Bacaan</a></div>
<div class="admin-table-wrap mt-5"><table class="admin-table"><thead><tr><th>Bacaan</th><th>Gerakan</th><th>Kategori</th><th>Audio</th><th>Sumber</th><th>Aksi</th></tr></thead><tbody>@forelse($items as $item)<tr><td><strong>{{ $item->judul }}</strong><small dir="rtl" lang="ar" class="arabic-text block max-w-xs truncate text-lg">{{ $item->teks_arab }}</small></td><td>{{ $item->gerakan?->nama }}</td><td>{{ $item->gerakan?->kategori?->nama }}</td><td>@if($item->audio_src)<audio controls preload="none" src="{{ $item->audio_src }}" class="w-48"></audio>@else<span class="text-slate-400">Tidak ada</span>@endif</td><td><span class="line-clamp-2 max-w-xs">{{ $item->sumber }}</span></td><td><div class="table-actions"><a href="{{ route('admin.bacaan.edit',$item) }}" class="btn-table">Edit</a><form method="POST" action="{{ route('admin.bacaan.destroy',$item) }}" onsubmit="return confirm('Hapus bacaan dan audio ini?')">@csrf @method('DELETE')<button class="btn-table danger">Hapus</button></form></div></td></tr>@empty<tr><td colspan="6" class="text-center text-slate-500">Belum ada bacaan.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $items->links() }}</div>
@endsection
